const squareGame = (whereInsert, numSquare, nameclass) =>{
    for (let i = 0; i < num; i++){
        whereInsert.innerHTML += `<div id="${nameclass}">${i}</div>`
    }
}


const outerSquare 

const easy = document.querySelector(".LevelEasy");
const medium = document.querySelector(".LevelMedium");
const hard = document.querySelector(".LevelHard");

easy.addEventListener("click", function(){
    squareGame(outerSquare, 100, squareEasy)
});